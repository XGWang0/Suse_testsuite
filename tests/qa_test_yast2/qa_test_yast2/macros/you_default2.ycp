// YaST2 UI macro file generated by UI macro recorder
//
//     Qt UI: Alt-Ctrl-Shift-M: start/stop Macro recorder
//            Alt-Ctrl-Shift-P: Play macro
//
// Each block will be executed just before the next UserInput().
// 'return' before the closing brace ( '}' ) of each block relinquishes control
// back to the YCP source.
// Inside each block arbitrary YCP code can be added manually.

{
    {
        // Dialog with YQWizard "Willkommen zum YaST-Online-Update"
        //
        // 2005-08-30 14:47:36

        UI::ChangeWidget( `id (`server),	`Value,	1 );	// YComboBox "Installationsquelle"
        UI::ChangeWidget( `id (`url),	`Value,	"http://ftp.hosteurope.de/mirror/ftp.suse.com/pub/suse/" );	// YTextEntry "Pfad"
        UI::ChangeWidget( `id (`manual),	`Value,	true );	// YCheckBox "Manuelle Auswahl von Patches"
        UI::ChangeWidget( `id (`reload),	`Value,	false );	// YCheckBox "Alle Patches vom Server neu laden"

        UI::FakeUserInput( `next );

	SCR::Execute(.background.run_output, "/usr/share/qa/qa_test_yast2/helpscripts/call_xmacro 12 /usr/share/qa/qa_test_yast2/xmacro/xmacro_alt_a ");

        return;
    }


    {
        // Dialog with YQWizard "Patches herunterladen und installieren"
        //
        // 2005-08-30 14:47:39

        UI::ChangeWidget( `id (`remove_packages),	`Value,	true );	// YCheckBox "Quellpakete nach dem Update entfernen"

        UI::FakeUserInput( `next );

        return;
    }

}
