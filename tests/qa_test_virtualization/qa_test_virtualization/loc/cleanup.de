#!/bin/bash

MOUNT_DIR="/MOUNT-VIRT"
MOUNT_DIR2="/MOUNT-VIRT2"

# Temporal disable - can break the connection
#echo "Killing processes that still access $MOUNT_DIR and $MOUNT_DIR2 (if any)..." >&2
#fuser -km $MOUNT_DIR
#fuser -km $MOUNT_DIR2

umount $MOUNT_DIR && rmdir $MOUNT_DIR
umount $MOUNT_DIR2 && rmdir $MOUNT_DIR2 

