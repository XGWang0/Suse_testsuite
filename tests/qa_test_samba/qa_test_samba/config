# This is configuration file for qa_samba package

# If some samba instances are running, it may interfere with the test
# testsuite tries to end them dracefully (/etc/init.d/XXX stop). But
# it might not be enough, When the following value is set to 'yes'
# testsuite will also do 'killall -9 XXX' when the service fails to 
# terminate in response to normal commands
HARDKILL_RUNNING_INSTANCES_AT_START="yes"

